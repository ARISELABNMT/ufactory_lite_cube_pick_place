# Images

`cube_detection_example.png` is a real capture from this project's camera showing `cube_detector`
correctly boxing the target cube (see main README § Detection Tuning).

Add your own photos/screenshots here and reference them from the main [README.md](../../README.md)
(look for the `<!-- TODO: image -->` markers). Suggested shots:

| Filename (suggested) | What to capture |
|---|---|
| `robot_setup.jpg` | The physical xArm Lite6 + table + cube + camera, wide shot |
| `robot_gripper_closeup.jpg` | Close-up of the vacuum gripper holding the cube |
| `rviz_realmove.png` | RViz during a real-hardware run — planning scene, robot model, camera view panel |
| `rviz_gazebo.png` | RViz + Gazebo side by side during a simulation run |
| `web_ui_dashboard.png` | The web UI (`urxp_web_ui`) full-screen, mid-task |
| `web_ui_camera_view.png` | The web UI's camera panel showing the detection overlay |

Keep images reasonably sized (compress large photos — a few hundred KB each is plenty for a
README) so the repo stays light to clone.
