# Images

`cube_detection_example.png` is a real capture from this project's camera showing `cube_detector`
correctly boxing the target cube (see main README § Detection Tuning).

`web_ui_dashboard.png` and `rviz_realmove.png` are in and linked from the main README already.

Add more photos/screenshots here and reference them from the main [README.md](../../README.md)
(look for the remaining `<!-- TODO: image -->` marker). Still useful to add:

| Filename (suggested) | What to capture | Status |
|---|---|---|
| `robot_setup.jpg` | The physical xArm Lite6 + table + cube + camera, wide shot | ⬜ TODO |
| `robot_gripper_closeup.jpg` | Close-up of the vacuum gripper holding the cube | ⬜ TODO |
| `rviz_realmove.png` | RViz during a real-hardware run — planning scene, robot model | ✅ done |
| `rviz_gazebo.png` | RViz + Gazebo side by side during a simulation run | ⬜ TODO |
| `web_ui_dashboard.png` | The web UI (`urxp_web_ui`) full-screen, mid-task | ✅ done |
| `web_ui_camera_view.png` | The web UI's camera panel showing the detection overlay | ⬜ TODO |

Keep images reasonably sized (compress large photos — a few hundred KB each is plenty for a
README) so the repo stays light to clone.
